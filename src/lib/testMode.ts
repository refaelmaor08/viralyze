/**
 * UNLIMITED TEST MODE
 *
 * When true:
 *   - All plan limits are bypassed (duration, analyses, credits)
 *   - The analyze button is always enabled
 *   - Credits are never decremented
 *   - No upgrade blockers are shown
 *
 * Set to false before production launch to re-enable billing enforcement.
 */
export const UNLIMITED_TEST_MODE = true;
